spring:
  application:
    name: nacos-config-client

  cloud:
    nacos:
      config:
        server-addr: 47.111.114.208:8848
        username: nacos
        password: nacos
        file-extension: yml

server:
  port: 9600